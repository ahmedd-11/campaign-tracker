spring.application.name=campaigntracker

spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/campaigntracker}
spring.datasource.username=${DATABASE_USERNAME:ahm}
spring.datasource.password=${DATABASE_PASSWORD:}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false